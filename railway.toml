[build]
builder = "nixpacks"
watchPatterns = ["golf-ryder-cup-web/**"]
buildCommand = "cd golf-ryder-cup-web && npm ci && npm run build"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
startCommand = "cd golf-ryder-cup-web && npm start"
numReplicas = 1

[service]
internalPort = 3000
